%p
  An alert has been triggered in #{@project.full_path}.

%p
  Environment: #{@environment.name}

%p
  Metric:

  %pre
    = @alert.full_query

%p
  = link_to("View #{@environment.name} performance dashboard.", metrics_project_environment_path(@environment.project, @environment))
